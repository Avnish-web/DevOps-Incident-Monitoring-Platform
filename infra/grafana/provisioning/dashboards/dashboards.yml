apiVersion: 1

providers:
  - name: monitoring-platform
    folder: Monitoring Platform
    type: file
    # Dashboards are code: edit the JSON in the repository, not in the UI.
    allowUiUpdates: false
    disableDeletion: true
    options:
      path: /var/lib/grafana/dashboards
